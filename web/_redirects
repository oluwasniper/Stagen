# Cloudflare Pages redirect rules
# Privacy policy is a standalone page — exclude from SPA fallback
/privacy-policy    /privacy-policy/index.html    200
/privacy-policy/   /privacy-policy/index.html    200

# Flutter web SPA fallback — must be last
/*    /index.html    200
